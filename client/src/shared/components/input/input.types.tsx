export type InputTypes = 'text' | 'number' | 'range' | 'password' | 'switch';

export enum InputType {
  TEXT = 'text',
  PASSWORD = 'password',
  NUMBER = 'number',
  RANGE = 'range',
  SWITCH = 'switch',
}
